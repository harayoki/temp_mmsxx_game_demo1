# TODO

まだ手を付けていない課題だけを書く。着手して終わったらここから消し、
仕様は [ENGINE.md](ENGINE.md) / [UTIL.md](UTIL.md) /
[STAR_FABLE.md](STAR_FABLE.md) / [BOSSES.md](BOSSES.md) へ移す。

> この表を作ったあとに方針が変わったものは、**新しい指示のほうを採用**して
> 古い項目は消してある。

---

# A. ボスまわり(残り)

ラスボスの第 2 段階(攻撃・崩し・瞑想・撃破の演出)、カニの脚、
BIG MOAI の流れ、16t のおもり、未実装さんの命ごいは実装済み。
仕様は [BOSSES.md](BOSSES.md) / [STAR_FABLE.md](STAR_FABLE.md) を参照。

- **裂け目のまわりに細かいひび**を散らしたい(裂け目そのものは実装済み)
- 第 1 段階の 256 発は、通しで遊んでから見直す
- ドラゴン(KING OARFISH)の**ひげもピンク**にする(冠はピンク済み)
- BIG MOAI の**合体するときの SE**
- BIG MOAI: 合体後(E)は外側からでもふつうに倒せる。
  「内側から壊す」意味が上下が寄る 5 秒だけになっているので、通しで遊んで判断する

---

# D. 出現の排他制御

でかい敵は基本的に排他にする。

| 相手 | 同時に出てよい数 |
|---|---|
| 小惑星 | 3 つまで |
| ミサイル | 3 本まで |
| モアイ | 1 |
| 目玉 | 1 |

- **ロケット(ミサイル)が出ているときは岩を出さない**
- 目玉と小惑星は排他(実装済み)
- **モアイか目玉のどちらかは、少なくともステージに 1 回は出す**

---

# E. 通常モード(やさしいほう)

- **左右から同時に挟み撃ちしてくる敵が出ないようにしたのに、たくさん出る**(要修正)
- 出すとしても **通常では速度を遅く**する
- **シールドはアイテムを 1 回取ると MAX まで上がる**。減っているときも MAX まで回復
- **後ろから来る敵は、左右の端からしか来ない**ようにする

---

# F. UI・音まわり

- **オーブが集まったときに BGM の再生がやり直しになる**(同じ曲なのに頭に戻る)。
  正しくは**フェードアウトが始まる**、が期待動作
- **未実装君の涙の出る位置が目と合っていない**
- **`MEIJIN` コマンドでラピッドも最大**にする

## 合成音色(まだ仕様は決めていない)

**1 つの名前で、複数の音を重ねて鳴らせるようにしたい。**

いまは重ねたい音の数だけトラックを書いている。`beat`（[demotunes.js](../engine/util/demotunes.js)
の `beatTune()`）では、FM の打楽器が細いのでノイズと矩形波を同じ位置に置いており、
**同じリズムを 3 回書く**ことになっている。和音も 1 本では鳴らせないので
3 トラックに分かれている。これを音色の側で持てれば、MML はぐっと短くなる。

重ねたいものの例:

- 打楽器 + ノイズ(胴鳴り・空気)
- 基音 + 倍音(オクターブ上、5 度上)を混ぜて厚みを出す
- 同じ音を少しずらして重ねる(デチューンは `@d` で済むが、音色ごと変えたい)

決めていないこと: 重ねる音ごとの音量差・高さのずらし・エンベロープを
別々に持てるか、トラック数(発音数)の数えかたをどうするか。

## ゲームオーバーと名前入力

- **ボスラッシュで全部倒したとき、ランクインしていれば
  ゲームオーバー画面を通らずに名前入力へ**。していなければゲームオーバー

---

# G. 演出

- **タイトルの背景をブラックホール**にする

---

# H. キャラクター紹介

- **目玉は単独ページ**にする
- 図鑑の絵が**古いままのページ**がないか見直す(ゲーム中の姿と合わせる)

---

# I. デバッグ

- **ランクインしそうな場面の自動キャプチャ**(`capture/` に 10 枚)は
  SNS シェア用の絵を試すための仮のしくみ。要らなくなったら
  `main.js` のキャプチャの節と `serve.js` の `/__capture` を消す
  (**ALT + S** のクリップボードへのコピーは公開版でも使う機能なので、こちらは残す)

---

# J. エンジン(スマホ対応を始めてから着手する)

> J-1 〜 J-3 は**スマホ対応に取りかかるとき**にまとめてやる。それまでは触らない。


## J-1. 表示スケールの選択

- 既定は **1 / 2 / 3 倍から選ぶ**(整数倍なのでピクセルパーフェクト)
- 別に「**画面に合わせて伸ばす**」も用意する
- 端末の画面サイズから既定値を決め、あとから設定で変えられるようにする
- `devicePixelRatio` を見て、canvas の実ピクセルと CSS サイズを分けて扱う

```js
mmsxx.setScale(2);          // 整数倍
mmsxx.setScale('fit');      // 画面に合わせて伸ばす
```

## J-2. バーチャルパッド(タッチ操作)

**設計は [SMARTPHONE.md](SMARTPHONE.md) にまとめた**(別セッションで実装する)。
決まっているのは次のとおり。

- GUI は **DOM**。横画面の左右の空きに置く(ゲーム画面には重ねない)
- 十字は **位置が移動する相対型**(触れたところが原点)
- ショットは **こすり打ち**。80 年代シューティングのオマージュなので、
  **連射は腕前のままにする**(押しっぱなしの自動連射にはしない)。
  **領域に入ったら PRESS・出たら RELEASE**(キーボードと同じ扱い)。
  領域の割りかたは**未定**
- **PC ではキーボードを残す**。スマホでも**文字の打ち込みは素通し**にする
  (タブレットに繋いだキーボードで名前を打てるように)
- 縦持ちのときは **90 度回して横で見せる**(「横にしてください」とは出さない)
- エンジンの `Input` に押す / 離すの口を足すだけ。
  **ゲーム側は PC 用のコードのまま**動かし、差はエンジン側で吸収する

**土台はできている**(SMARTPHONE.md の 2 節)。残っているのはパッドそのもの。

- 済: `Input.press()` / `release()` / `clear()`、行送りの `repeat()`
- 済: 端末の見分け `engine/util/device.js`(`?device=` で上書き)
- 済: ALT の代わりになる DOM のボタン(ミュート、共有の SAVE IMAGE)
- 未: 十字とショットのボタン、画面座標 → 仮想座標の逆変換(V2 で作る)

未決: **こすり打ちのやりかたと伝えかた**、長押しの扱い、ポーズ以外のボタン、
スマホでのキーボード入力。

## J-3. タッチ環境での文字入力

裏技(ポーズ中に打ち込み)をどうするか。名前入力は対応済み。

1. **画面内キーボード**を出す(本命)
2. 裏技はタッチ環境でも**コマンド一覧は出さない**(答えを見せてしまうため)。
   「**なにか入力できそうだな**」と分かる程度の入力欄を出すだけにする

## K. シェア機能

遊んだ画面を SNS へ出せるようにする。**まだ SNS へつなぐところは作らない**
(いまは「OK」で閉じるだけ)。

| 決めごと | 内容 |
|---|---|
| 始めかた | ゲーム中に**特殊キー**で始動。**ハイスコアを出したときは自動で始動** |
| 画面 | その場の画面をキャプチャして **2 倍**に加工する |
| ゲーム中の扱い | 始動した時点で**ポーズ**する |
| 出しかた | canvas の上に**ダイアログ**を重ねて、画像とシェア文言を見せる |
| 文言 | **英語 1 本**。ハイスコアのときだけ言うことが変わる |
| ボタン | `POST TO X`(**まだ繋いでいない**) / `SAVE IMAGE` / `SAVE VIDEO` / `CLOSE` |

- 画像は `mmsxx.capture({ scale: 2, type: 'canvas' })` でそのまま取れる
  (すでに Alt+S のクリップボード保存で使っている道)
- ダイアログは DOM で作る(canvas の中に描かない)。
  バーチャルパッドと同じ考えかた([SMARTPHONE.md](SMARTPHONE.md) の 5 節)

### いまの作り(できあがっている)

`game/main.js` の「シェア」の節にまとまっている。

- 出すきっかけは **ALT+P**(どの画面でも)と、**ランクインしたときの自動**
- 見せる絵は、溜めてあるコマ(K-2 の仕組み)の中の 1 枚。
  **左右で選び直せる**(◀ ▶ のボタンと ← → キー。溜まっているぶん全部から選べる)
- 板の中の押せるものは **← → で選び、SPACE で実行、ESC でとじる**。
  マウスのクリックでも同じ。X の絵は置いてあるが**まだ繋いでいない**
- 「動画を保存」は、リプレイのときに録ったものを手元へ落とす

### あとでエンジンへ移すもの(まだやらない)

**ビュー(見せかた)はエンジンの util へ、文言はゲームに残す。**
スマホ対応でエンジン側に「canvas の上に板を重ねて、キーやタッチをゲームへ流さない」
土台ができるはずなので、そこへ乗せ替える。

| どこへ | 何を |
|---|---|
| エンジン(util) | 板の枠、押せるものを左右で選ぶ仕組み、コマ選び、画像の受け渡し、ダウンロード |
| ゲーム | 文言(何を書くか)、どの場面で出すか、どの絵を最初に見せるか |

- 同じ理由で、クリップボードへのコピー(`captureClipboard()`)と
  開発サーバへの書き出しもエンジン側が持つほうが収まりが良い

## K-2. プレイ動画

遊んだところを動画にして出せるようにする。**まだ着手していない**。

| 決めごと | 内容 |
|---|---|
| 録りかた | `MediaRecorder` + `canvas.captureStream()`。**外部ツールも許可も要らない** |
| 音 | ミュートで作ったマスターから分岐して混ぜる |
| 大きさ | 3 倍のオフスクリーンへ毎コマ写してから録る(264x200 のままでは小さい) |
| 公開版 | **公開版でも使える**。webm が作れない環境(Safari)ではボタンを出さない |
| 長さ | やられる直前の 30 秒。上限も決める |

### 直前の 30 秒をどう残すか

webm は**先頭の切れ端だけが「何の動画か」を持っている**ので、古い切れ端を捨てる
やりかたでは再生できないファイルになる。手は 2 つ。

1. **録画を 15 秒ずらして 2 本走らせる**（すぐできる）。どの瞬間でも
   どちらかが 15〜30 秒ぶんを 1 本の正しいファイルとして持っている
2. **操作を記録して再生する**（きれい。長さもきっかり決まる）。
   そのためには乱数を種つきにする必要がある

### 乱数を種つきにする(1 と 2 の両方に効く)

- **エンジン側に持たせる**。`mmsxx.rng.seed() / next() / int() / save() / restore()`
- **ゲームに効くところだけ**種つきにする。爆発の粒や画面揺れなど**見た目はそのまま**
  (`main.js` の `Math.random()` 101 か所のうち、効きそうなのは 34 か所ほど)
- 記録に残すのは `{ seed, build, keys: [[コマ番号, キー], ...] }` だけ。30 秒で数キロバイト

**決めごと**:

- 記録は**その場で動画にするための一時的なもの**。長く取っておくものではない
- 記録に**版番号(`BUILD.version`)を入れる**。違う版なら再生を断る
  (コードが 1 行変わるだけで再現できなくなり、しかも**黙って壊れる**ため)

### いまの作り(できあがっている)

**画面を溜める方式**にしてある。エンジンが**色番号のまま**輪っかに溜め、
ゲームは何秒ぶん持つかを決めるだけ。

```js
mmsxx.keepFrames(3);   // 直前 3 秒(ゲーム側は SHARE_KEEP_SEC の 1 か所)
mmsxx.frameAgo(1);     // 1 秒前にいちばん近いコマ
mmsxx.frameBackCanvas(0);   // 番号で取り出す(リプレイ用)
```

- 1 コマ 51.6KB、**60fps で 3 秒なら 9.1MB**（色に直したものの 4 分の 1）
- **60fps で溜める**。30fps だと点滅するキャラが消えたコマを拾ってしまう
- シェアの 1 枚もここから取る（`frameAgo(1)`）
- **リプレイの再生（溜めたコマを順に出す）はまだ作っていない**

### 先の話 — 状態の写しで置き換えられる

**状態の写し（乱数の進み具合を含む）+ そこからのキー操作**があれば、
画面を溜めなくても最後の数秒を作り直せる。**数十キロバイトで済む**。

| | 画面を溜める（いま） | 状態の写し |
|---|---|---|
| 大きさ | 9.1MB | 数十 KB |
| 音 | 鳴らない（絵だけ） | **鳴る**（作り直すので） |
| 見た目 | **見たままと完全一致** | 爆発の粒などは少しちがう |
| 手間 | ゼロ | 種類ごとに書き出しと組み立てが要る |
| 続きを遊ぶ | できない | **できる** |

**引っかかるのは、状態にスプライトの実体が混ざっていること。**
敵も弾も `{ sp, vx, vy, ... }` の形でスプライトそのものを抱えているので、
写すときは「どの絵か・どこにいるか」へ開いて、戻すときに作り直す必要がある。
しかも**ゲームを直すたびに手当てが要る**（忘れると黙って壊れる）。

**進めかた**:

1. いま … 画面を溜める（できあがり）
2. 次 … 状態の写し（**続きを誰かに遊んでもらう**ため。こちらが本題）
3. そのとき … リプレイも写し方式へ寄せて、画面の溜めをやめる

スマホで 9.1MB が重いと分かったら、2 を前倒しする理由になる。
乱数を種つきにしてあるので、2 の半分はすでに済んでいる。

## L. 絵の書き出しと取り込み

### いまの作り(できあがっている)

**絵を画像として書き出す**道具(`engine/util/artexport.js`)。開発用。

```js
exportSymbol(mmsxx, SPRITE_SYMBOLS.player, { scale: 4 });     // 1 枚
exportSheet(mmsxx, SPRITE_SYMBOLS, { scale: 2, width: 512, padding: 4 });  // まとめて
downloadArt(canvas, 'sheet.png');                              // 落とす
```

開発版のコンソールからも呼べる。

```js
mmsxxArt('player', 4)           // 1 枚を 4 倍で
mmsxxSheet('sprite', 2, 512)    // スプライトを 2 倍で 512 ドット幅に並べて
mmsxxSheet('bg', 1, 1024)       // BG も同じように
```

- 倍率は整数倍(ドットはぼかさない)
- まとめて並べるときは**いちばん大きい絵に合わせた升目**に置く。余白と幅を決められる
- 色は**画面と同じパレット**から引く。色合いを切り替えて書き出せば、その色のまま出る
- 透明(色番号 0)は透けたまま残る

### V2 でやること

- **名前を焼き込む**。一覧として見るときに、どれが何か分かるようにしたい
  (いまは絵だけ。字を入れる場所と大きさの決めごとが要る)
- **外で直した絵を取り込み直す流れ**。書き出す → 外の道具で直す → 取り込む、をひと続きにする
- **フルカラーの絵を取り込んで変換する流れ**。減色と「横 8 ドット 2 色」への均しを、
  取り込みのところでやる(いまは makedata.mjs にコードとして描く方式だけ)

## J-4. そのほか

(音・画面の向き・取りこぼし対策は [SMARTPHONE.md](SMARTPHONE.md) にも書いてある)

- 音: iOS では最初のタッチで `audio.unlock()` を呼ぶ必要がある
- 画面の向き: **横画面前提**。縦画面のときは「横にしてください」と出す
- スマホのセンサー: **ジャイロ・位置情報**を触れるようにしたい(まだやらない)
- 描画: 中身のあるレイヤーは毎フレーム全ピクセルを走査しているので、
  もっと軽くしたくなったら「変化した範囲だけ合成」か WebGL 化を検討する
