// ビルドの種類。**手元のソースはいつも開発版(dev: true)**。
// web へ上げる版は build-deploy.ps1 がこのファイルを dev: false で書き換える。
//
// 場所(localhost かどうか)ではなくビルドで決めるので、
// 公開版を手元のサーバで開いても、開発用の機能は出てこない。
// 逆に、開発版をスマホなどへ持っていっても開発用の機能は使える。
export const BUILD = {
  /** 開発用の機能(シーン選択・コンソール関数・画面の保存など)を入れるか */
  dev: true,
  /** コンソールにロゴを出すか(build-deploy.ps1 -NoLogo で false になる) */
  logo: true,
  /** どのビルドか(画面に出したいときのため) */
  name: 'local',
};
